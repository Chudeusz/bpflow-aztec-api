# BPFlow-Aztec-API

```
$ npm install
$ npm install -g nodemon
$ nodemon bin/www dla dev
Screen: $ screen -S nodemon bin/www na linux
$ node bin/www produkcja bez aktualizacji pliku app.js na żywo
```